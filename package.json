{
  "name": "event-management-platform",
  "version": "1.0.0",
  "description": "Event Management Platform with Node and React.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "watch": "nodemon server.js",
    "client": "cd client && npm run dev",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": "20.x"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ArcLKN/eventManagementPlatform.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ArcLKN/eventManagementPlatform/issues"
  },
  "homepage": "https://github.com/ArcLKN/eventManagementPlatform#readme",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.6.1",
    "react-router-dom": "^6.23.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
